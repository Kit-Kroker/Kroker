# Tasks Requirements-Quality Checklist: Frontend restructure — by-screen features, design-system port, board screen

**Purpose**: Unit tests for `tasks.md` — coverage of every FR/SC/G-ruling and plan task, RED-before-GREEN ordering, dependency and [P]-mark soundness, per-task executability without extra context, and consistency with the approved plan, skeptic dispositions and spec amendments. Tests the task list's writing, not the implementation.
**Created**: 2026-09-23 (tasks-domain run, reviewer seat)
**Feature**: [../tasks.md](../tasks.md) · [../spec.md](../spec.md) · [../plan.md](../plan.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements.

## Coverage Completeness

- [ ] CHK001 - Does every FR (incl. 007a, 009a, 020a, 020b, 021a, 023a), SC and G1–G9 ruling map to at least one named task? [Completeness, tasks.md all phases]
- [ ] CHK002 - Does every plan task (A0–A5, B1–B6 incl. B1b, C1–C9) have a corresponding tasks.md task, with RED/GREEN splits traceable back to it? [Traceability, plan §Task groups]
- [ ] CHK003 - Does every task map to a requirement, story or plan task (no orphan tasks), including probe/spike tasks? [Traceability]
- [ ] CHK004 - Is the parked G2b task stated as non-gating in the task list itself and excluded from the dependency chain? [Dependency, tasks.md T024]

## Ordering & Test-First Protocol

- [ ] CHK005 - Is RED-before-GREEN held per the stated group protocol (A: boundary guard + count preservation; B: clause doc and tests copied first, failing for the stated reason; C: RED test task before each implementation task)? [Ordering, tasks.md §Tests]
- [ ] CHK006 - Is the group order A → B → C binding, and are intra-group dependencies (B2 before StatusPip importers; slots before button; store+adapter before tab view) stated and honoured? [Ordering, tasks.md §Dependencies]
- [ ] CHK007 - Is every task's gate reachable at its position in the order — no task gated "green" on an artifact a later task creates? [Consistency, tasks.md T036–T039 / §Dependencies]
- [ ] CHK008 - Does each Group-B port's RED step fail for the stated reason (missing module/registration), per the port recipe? [Ordering, tasks.md §Port recipe]

## Parallelism & [P] Marks

- [ ] CHK009 - Are [P] marks consistent with their own definition ("different files, no dependency on an incomplete task") across the port batch, given identical dependency shapes? [Consistency, tasks.md T017/T018/T021]
- [ ] CHK010 - Is the shared-file serial-merge guidance (every port edits `registry.ts` and `index.ts`) stated where the [P] marks are defined, and sufficient for a worktree-per-task merge? [Clarity, tasks.md §Format / T-note]

## Executability Without Extra Context

- [ ] CHK011 - Does every task name exact paths (FE/UI/PROTO abbreviations, backend files, test files) with no lookups left to the executor? [Clarity, tasks.md all]
- [ ] CHK012 - Do all gates name their commands — `python scripts/check_ui.py` only for JS, one pytest invocation per call, ruff/mypy/file-size for the backend slice? [Verification, tasks.md §Standing rules / T026]
- [ ] CHK013 - Are exact locators and assertions quoted in tasks (e.g. T009's `#showcase-button-primary` / "Run this version") verifiable against the sources they cite? [Accuracy, tasks.md T009 / PROTO button.profiles.ts]

## Consistency With Plan, Dispositions and Fixes

- [ ] CHK014 - Does no task contradict a skeptic disposition (1–10) or a round-1 plan fix (.gitkeep lifecycle, status.ts placement, pulsing rule, FR-003 correction)? [Consistency, dispositions / reviewer-002-plan-2.md]
- [ ] CHK015 - Is every documentation update attached to the task that changes the facts it records (screen-table Run row when the route component switches)? [Consistency, tasks.md T006/T037]
- [ ] CHK016 - Do the standing rules encode the plan's traps (R4 toolchain, vi.mock rewrites, dump-script, stale preview servers, clause-report reading, no attribution trailers)? [Completeness, tasks.md §Standing rules]

## Verification & Gates

- [ ] CHK017 - Is the count-preservation arithmetic stated wherever tests are split or moved (T002, T004, T005), matching R2's "no test lost"? [Measurability]
- [ ] CHK018 - Does each group end with a checkpoint/close-out task matching R3 (both workspaces, both Playwright tiers) and the quickstart section it names? [Verification, T006/T023/T040]
- [ ] CHK019 - Is each SC verified by a named task (SC-002 baseline T001; SC-003 planted violations T002; SC-004 clause report T023; SC-005/006 app tier T039; SC-007 T040)? [Traceability]
- [ ] CHK020 - Is the MVP statement (Phase 2 shippable alone) consistent with the story priorities and the group gates? [Consistency, tasks.md §Implementation strategy]

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline
- Companion checklists: `requirements.md`, `spec.md`, `plan.md` (untouched by this run)

## Round 2 (2026-09-23, reviewer re-review)

Round-1 findings re-verified in tasks.md after the planner's fixes (`reviewer-002-tasks-fixes-1.md`, now T001–T041):
- CHK007 resolved — T036 RED is `RunView.tabs.test.ts` only; T037 GREEN is the tab host plus the R-4 props function on the still-RunView route (no BoardTab import, gate reachable); T038 RED adds `BoardTab.test.ts` + `RunPage.test.ts` before T039 GREEN creates both modules, switches the router and updates the AGENTS.md Run row. Dependency line matches; app tier renumbered T040, close-out T041.
- CHK009/CHK010 resolved — T017 now [P]; the [P] definition names the shared `registry.ts`/`index.ts` serial-merge exception; Phase 3 dependency line simplified ("T017, T018, T021 need T008 — already satisfied").
- CHK015 resolved — T006 states the temporary Run row and names T039 as the switch; T039 carries the AGENTS.md row update.
- CHK005 (Group C test-first) — now uniform across T025→T026 through T038→T039.
