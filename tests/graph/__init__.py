# Package marker (as tests/replay): pytest's default import mode keys test
# modules by basename, and tests/graph hosts same-named files that exist at
# tests/ level too (test_graph_start.py, E-77 T020) — the package name keeps
# them distinct.
